# Keepin It Realty – Site (draft)

Static site scaffold modeled after the screenshots you provided (dark nav, serif typography, hero with olive rounded overlay, featured listings section, footer contact block).

## Run locally
Just open `index.html` in a browser.

## Deploy
Works on any static host (Cloudflare Pages / Netlify / S3). Build step not required.

## Next inputs needed
- Final brand name + logo file (SVG/PNG)
- Hero images (desktop + mobile)
- Contact details (phone, email, office address)
- IDX/Search destination URL (where “Search Homes Now” should point)
- Featured listing feed source (manual list vs IDX embed)
