# 2026-03-05

## OpenClaw local (Windows) install — troubleshooting notes
- Initial installer failure: PowerShell execution policy blocked `npm.ps1`/`openclaw.ps1` ("running scripts is disabled"). Fix:
  - One-shot: `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`
  - Persistent (user): `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`
- Node was installed (v24.14.0) and npm (11.9.0) present; verified `npm-cli.js` exists under `C:\Program Files\nodejs\node_modules\npm\bin\npm-cli.js`.
- Global install initially failed with `npm ERR! spawn git ENOENT` → installed Git (`winget install --id Git.Git -e`) and re-ran.
- OpenClaw installed successfully: `openclaw --version` returned **2026.3.2**.
- `openclaw gateway status` initially showed Scheduled Task missing; ran `openclaw gateway install` (Admin) which created Scheduled Task **OpenClaw Gateway** and generated a gateway token saved to `C:\Users\17033\.openclaw\openclaw.json`.
- Gateway task existed but exited immediately (Task last run result `1`). Manual run of `C:\Users\17033\.openclaw\gateway.cmd` showed:
  - `Cannot start: scheduled task gateway mode local (current: unset) or pass --allow-unconfigured.`
- Next suggested command: `openclaw gateway start --allow-unconfigured`, then `openclaw gateway status`.
- Logs encountered:
  - CLI/status log at `C:\Users\17033\AppData\Local\Temp\openclaw\openclaw-2026-03-05.log`
  - Config audit at `C:\Users\17033\.openclaw\logs\config-audit.jsonl`

## User request
- Caligula asked to save this chat and put it in storage for now.

## Knowledge management / storage
- Built a two-way filing system in `memory/`:
  - `memory/INDEX.md` (By Date + By Topic)
  - Topic docs: `openclaw-windows.md`, `icarusfoundry.md`, `calling-bot.md`, `ops-rails.md`
- User preference: organize **both by date and by topic**.

## Icarus Foundry — entity formation (Florida)
- Created execution playbook: Sunbiz LLC filing → EIN → IRS Form 2553 (S-election now) → bank/bookkeeping/payroll → contractor guardrails.
- Entity choice: **Florida LLC**.
- Ownership: **single-member** (no other owners).
- Registered agent: will use a **service**; selected **Northwest Registered Agent**.
- Cost-effective alternatives discussed: **Harbor Compliance (~$99/yr)**, **Corporate Filing Solutions / FilingsMadeEasy (~$95/yr)**; noted ultra-cheap options exist but may be riskier.
- S-Corp election: user wants **S-election effective now** (LLC taxed as S-Corp via IRS Form 2553).
- Virtual office address for principal/mailing: **333 Las Olas Way CU 1, Fort Lauderdale, FL 33301**.
- Contractors likely in next 60 days: **legal + accounting**.

## Brave API key / Docker context
- User runs Hostinger VPS with Docker; OpenClaw present inside container (host shell lacked `openclaw`).
- Container identified as `openclaw-wqui-openclaw-1` (id prefix `ecf0172629c1`).
- Guided user to `docker exec -it <container> bash` then `openclaw configure --section web`.

## System issue observed
- Web search tool unavailable due to missing Brave Search API key in this environment (error: `missing_brave_api_key`).
- Later, agent/runtime threw: `No API key found for provider "anthropic"` referencing `/data/.openclaw/agents/main/agent/auth-profiles.json`.
- User reported while configuring inside Docker that something disconnected the **OpenAI** key (suspected env/config/volume issue in container).
- Later confirmed **OPENAI_API_KEY is present in the container env** after re-adding.
- Brave web search confirmed working again after key setup (web_search returned results).
